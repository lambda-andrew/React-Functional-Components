import React from 'react';
import './Card.css';

const CardContent = () => {
    return(
        <div className='cardContent'>
            <h4>Get Started with React</h4>
            <p>React makes it painless to cerate interactive UIs. Design simple views for each state in your application.</p>
            <span>reactjs.org</span>
        </div>
    );
}

export default CardContent;